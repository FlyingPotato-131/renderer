#pragma once
#include <vector>
#include "structs.h"
#include "funcs.h"

std::vector<vec3> vertices();

std::vector<vec2> texture();

std::vector<normal> normal();

std::vector<indextriangle> triangle();
